# External junction location: session resolution + buffered upstream capture.
# Include from each junction location after setting junction_device and junction_prefix.

set $device_upstream "";

access_by_lua_file /etc/openresty/lua/junction/access.lua;
content_by_lua_file /etc/openresty/lua/junction/content.lua;
header_filter_by_lua_file /etc/openresty/lua/junction/header_filter.lua;
